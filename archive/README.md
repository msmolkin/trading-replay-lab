# Archive

Historical input is preserved here at the request of the repository owner. It is intentionally excluded from agent required reading and does not define requirements. Current requirements live in `README.md`, `docs/`, and `tasks/README.md`.
